# 飞书群机器人配置（示例，可提交）

复制本文件为 `feishu.local.env`，填入真实 Webhook，**不要把真实地址提交到 git**。

```bash
cp config/feishu.example.env config/feishu.local.env
```

```env
# 飞书群 → 设置 → 群机器人 → 自定义机器人 → Webhook 地址
FEISHU_WEBHOOK_URL=https://open.feishu.cn/open-apis/bot/v2/hook/xxxxxxxx-xxxx-xxxx-xxxx-xxxxxxxxxxxx

# 若创建机器人时开启了「签名校验」，填写密钥；未开启则留空
FEISHU_SECRET=
```

## 如何拿到 Webhook

1. 打开目标飞书群 → 设置 → 群机器人  
2. 添加 **自定义机器人**  
3. 安全设置建议至少开一种：关键词 / IP / 签名  
4. 若用关键词，可设为：`A股复盘`、`A股早盘`（或同时加旧词 `A股简报`）  
5. 复制 Webhook 地址到 `feishu.local.env`
